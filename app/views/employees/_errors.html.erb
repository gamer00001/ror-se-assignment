<% if errors.present? %>
  <div class="text-danger">
    <h2><%= pluralize(errors.count, "error") %> occurred: </h2>
    <ul>
        <% errors.each do |error| %>
            <li><%= error %></li>
        <% end %>
    </ul>
  </div>
<% end %>